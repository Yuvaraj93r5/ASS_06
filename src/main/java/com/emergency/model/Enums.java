package com.emergency.model;

public class Enums {
    public enum Priority { CRITICAL, HIGH, MODERATE, NORMAL }
    public enum AmbulanceType { BASIC, ADVANCED_LIFE_SUPPORT, ICU }
    public enum AmbulanceStatus { AVAILABLE, DISPATCHED, EN_ROUTE, PATIENT_PICKED_UP, HOSPITAL_ARRIVED }
}
